# Contributor: Asparagus Maximus <asparagus.maximus () gmail () com>

pkgname=swiftdove-intel64-pgo
_pkgname=swiftdove
_swiftver=_intel-pgo_x86_64_arch

pkgver=2.0.0.22
pkgrel=1
pkgdesc="Swiftdove is an optimized build of the Mozilla Thunderbird mail/news client for Linux."
arch=('x86_64')
url="http://swiftweasel.tuxfamily.org/"
license=('MPL')
depends=(mozilla-common desktop-file-utils nss libxt libstdc++5 libgnomeui)
provides=(swiftdove)
source=(http://downloads.sourceforge.net/swiftweasel/$_pkgname-$pkgver$_swiftver.tar.gz \
swiftdove.desktop)
#swiftdove-2.0.0.21_intel-pgo_x86_64-arch.tar.gz
md5sums=(0ced2edd1f58f8833167c925d2b93af4 \
d2505f6983625544e391ef874bef4835 )
         
package() {
  cd $startdir/src/
  mkdir -p $startdir/pkg/$pkgname/opt/
  cp -r $_pkgname $startdir/pkg/$pkgname/opt/$_pkgname
  mkdir -p $startdir/pkg/$pkgname/usr/bin
  ln -s /opt/$_pkgname/$_pkgname $startdir/pkg/$pkgname/usr/bin/$_pkgname

  mkdir -p $startdir/pkg/$pkgname/usr/share/applications
  mkdir -p $startdir/pkg/$pkgname/usr/share/pixmaps
  
  install -m644 $startdir/src/swiftdove.desktop $startdir/pkg/$pkgname/usr/share/applications/
  install -m644 $startdir/src/swiftdove/icons/mozicon50.xpm $startdir/pkg/$pkgname/usr/share/pixmaps/swiftdove.xpm
}

